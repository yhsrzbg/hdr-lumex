{
  "name": "hdr-lumex",
  "version": "3.0.0",
  "description": "A desktop HDR video analyzer for brightness, gamut, APL, MaxCLL and MaxFALL.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder",
    "build:win": "electron-builder --win",
    "build:mac": "electron-builder --mac",
    "build:linux": "electron-builder --linux"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@derhuerst/ffprobe-static": "^5.3.0",
    "ffmpeg-static": "^5.3.0"
  },
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.hdr-lumex.app",
    "productName": "HDR Lumex",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "renderer.js",
      "index.html",
      "assets/**/*",
      "src/**/*",
      "!**/node_modules/ffmpeg-static/ffmpeg*",
      "!**/node_modules/@derhuerst/ffprobe-static/ffprobe*"
    ],
    "win": {
      "target": "portable",
      "icon": "assets/icon.png",
      "extraResources": [
        {
          "from": "node_modules/ffmpeg-static/ffmpeg.exe",
          "to": "ffmpeg.exe"
        },
        {
          "from": "node_modules/@derhuerst/ffprobe-static/ffprobe.exe",
          "to": "ffprobe.exe"
        }
      ]
    },
    "portable": {
      "artifactName": "HDR-Lumex-${os}-${arch}.${ext}"
    },
    "mac": {
      "target": "dmg",
      "icon": "assets/icon.png",
      "artifactName": "HDR-Lumex-${os}-${arch}.${ext}",
      "extraResources": [
        {
          "from": "node_modules/ffmpeg-static/ffmpeg",
          "to": "ffmpeg"
        },
        {
          "from": "node_modules/@derhuerst/ffprobe-static/ffprobe",
          "to": "ffprobe"
        }
      ]
    },
    "linux": {
      "target": "AppImage",
      "category": "AudioVideo",
      "icon": "assets/icon.png",
      "artifactName": "HDR-Lumex-${os}-${arch}.${ext}",
      "extraResources": [
        {
          "from": "node_modules/ffmpeg-static/ffmpeg",
          "to": "ffmpeg"
        },
        {
          "from": "node_modules/@derhuerst/ffprobe-static/ffprobe",
          "to": "ffprobe"
        }
      ]
    }
  }
}
