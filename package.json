{
  "name": "hdr-video-analyzer",
  "version": "1.0.0",
  "description": "Interactive CLI tool for HDR video analysis - analyzes brightness, color gamut, and APL, then generates an HTML report",
  "main": "src/cli.js",
  "bin": {
    "hdr-video-analyzer": "src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "build": "node scripts/build-sea.cjs"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "postject": "^1.0.0-alpha.6"
  }
}
